@model System.Data.DataTable

@{
    ViewData["Title"] = "Daftar Mahasiswa";
}

<div class="jumbotron">
    <h1 class="text-center">@ViewData["Title"]</h1>
</div>

<button>
    <a asp-action="AddOrEdit">Create New</a>
</button>

<table class="table table-bordered">
    <thead class="thead-light">
        <tr>
            <th>
                NIM
            </th>
            <th>
                Nama Mahasiswa
            </th>
            <th>
                Tanggal Lahir
            </th>
            <th>
                Alamat
            </th>
            <th>
                Jenis Kelamin
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Rows.Count; i++)
        {
            <tr>
                <td>
                    @Model.Rows[i]["nim"]
                </td>
                <td>
                    @Model.Rows[i]["namaMhs"]
                </td>
                <td>
                    @Model.Rows[i]["tglLahir"]
                </td>
                <td>
                    @Model.Rows[i]["alamat"]
                </td>
                <td>
                    @Model.Rows[i]["jenisKelamin"]
                </td>
                <td>
                    <a asp-action="AddOrEdit" asp-route-id="@Model.Rows[i]["id"]">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@Model.Rows[i]["id"]">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
